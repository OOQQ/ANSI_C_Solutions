/* solution by OOQQ
 * The C Programming Language 2ed. ANSIc
 * chapter01, page034, program037, 1.21.c
 * -------------------------------------
 * write a program entab that replaces strings of blanks by the minimum number
 * of tabs and blanks to archieve the same spacing, use the same tab stops as
 * for program036
 */

#include <stdio.h>

int main(void) {

	printf(", whitespace =) %d, other = %d\n", nwhite, nother);

return 0;
}

