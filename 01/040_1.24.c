/* solution by OOQQ
 * The C Programming Language 2ed. ANSIc
 * chapter01, page034, program040, 1.24.c
 * -------------------------------------
 * write a program to check a C program for rudimentary syntax errors like
 * unbalanced parentheses, brakets and braces, dont forget quotes both single
 * and double, escape sequence characters and comments
 */

#include <stdio.h>

int main(void) {

	printf(", whitespace =) %d, other = %d\n", nwhite, nother);

return 0;
}

